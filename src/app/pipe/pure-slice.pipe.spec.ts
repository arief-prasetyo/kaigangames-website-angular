import { PureSlicePipe } from './pure-slice.pipe';

describe('PureSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new PureSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
