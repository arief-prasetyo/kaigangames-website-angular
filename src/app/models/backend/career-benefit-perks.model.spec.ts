import { CareerBenefitPerks } from './career-benefit-perks.model';

describe('CareerBenefitPerks', () => {
  it('should create an instance', () => {
    expect(new CareerBenefitPerks()).toBeTruthy();
  });
});
