import { CareerOpenPosition } from './career-open-position.model';

describe('CareerOpenPosition', () => {
  it('should create an instance', () => {
    expect(new CareerOpenPosition()).toBeTruthy();
  });
});
