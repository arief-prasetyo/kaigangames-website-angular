import { CommunityHighlights } from './community-highlights.model';

describe('CommunityHighlights', () => {
  it('should create an instance', () => {
    expect(new CommunityHighlights()).toBeTruthy();
  });
});
