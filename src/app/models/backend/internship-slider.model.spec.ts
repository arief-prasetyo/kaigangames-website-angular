import { InternshipSlider } from './internship-slider.model';

describe('InternshipSlider', () => {
  it('should create an instance', () => {
    expect(new InternshipSlider()).toBeTruthy();
  });
});
