import { CareerGroupOpenPosition } from './career-group-open-position.model';

describe('CareerGroupOpenPosition', () => {
  it('should create an instance', () => {
    expect(new CareerGroupOpenPosition()).toBeTruthy();
  });
});
